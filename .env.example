# Configurações do banco de dados
DB_HOST=localhost
DB_PORT=5432
DB_NAME=whisper_db
DB_USER=postgres
DB_PASS=postgres

# Configurações do modelo Whisper
# Opções: tiny, base, small, medium, large
WHISPER_MODEL=base

# Diretório para upload de arquivos
UPLOAD_DIR=./uploads

# Configurações da API Key padrão
DEFAULT_API_KEY_NAME=API Default
DEFAULT_API_KEY_EXPIRES_DAYS=365
DEFAULT_API_KEY_ALLOWED_IPS=
